---
import { LOCALE_COOKIE } from '../../lib/i18n';

Astro.cookies.set(LOCALE_COOKIE, 'en', {
  httpOnly: false,
  path: '/',
  sameSite: 'lax',
  secure: import.meta.env.PROD,
  maxAge: 60 * 60 * 24 * 365,
});

return Astro.redirect('/');
---
